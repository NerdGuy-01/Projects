// A custom command line shell 
#include<stdio.h>
#include<string.h> 
#include<stdlib.h>  
#include<windows.h>

#define LINE 1024
#define AGRS 64

int main(){
char command[LINE];
char *agrs[AGRS];
FILE *file;
file = fopen("file.txt","w");
if(file==NULL){
printf("File does not exist !!");
}
printf("myshell$");
fflush(stdout); // This removes all the buffer 
fgets(command,sizeof(of),stdin);
fprintf(file,"%s",command);

if(strcmp(command,"Exit")==0 || strcmp(command,"exit")==0){
  fclose(file);
  printf("Program ended");
}

else{
  while(1){
    // yet to write here 
  }
}


